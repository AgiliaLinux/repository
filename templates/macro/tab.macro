

{% macro tabs_head(headers=[]) %}
<div class="tabs_nav">
{% for title in headers %}
	<div class="tab_nav_item {% if loop.first -%} active
		{%- endif %}" id="tab_nav_item_{{ loop.index }}" data-tab-id="{{ loop.index }}">
		<span>{{ title }}</span>
	</div>
{% endfor %}
</div>
{% endmacro %}
